#include "random_graph_factory.hpp"

namespace graph::random {
BAGraph RandomGraphFactory::createBarabasiAlbertWithPreferentialAttachment(size_t nodes_number, size_t edges_number) {
  BAGraph graph;

  return graph;
}

BAGraph RandomGraphFactory::createBarabasiAlbertWithLinkSelection(size_t nodes_number, size_t edges_number) {
  BAGraph graph;

  return graph;
}

BAGraph RandomGraphFactory::createBarabasiAlbertWithCopyingModel(size_t nodes_number, size_t edges_number) {
  BAGraph graph;

  return graph;
}
} // namespace graph::random
